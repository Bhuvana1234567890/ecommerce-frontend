# E-Commerce Frontend

This is a simple frontend project structure for an online store.

## Folder Structure
- `assets/` → images and other assets
- `styles/` → CSS files
- `scripts/` → JavaScript files
- `index.html` → main entry point

## How to Run
Open `index.html` in your browser to view the project.
